# Go code vendor dependencies.
#
# package name will automatically be modified by the build process
# to match that of the application.
package: go-base


import:
    # add the application dependencies here
